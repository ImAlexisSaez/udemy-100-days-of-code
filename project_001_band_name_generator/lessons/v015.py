# Mala elección de nombres para las variables
n = input("What is your name? ")
l = len(n)
print(l)

# Los nombres deberían tener significado
name = input("What is your name? ")
length = len(name)
print(length)

# Variables con nombres compuestos a través de _
user_name = input("What is your name? ")
length = len(user_name)
print(length)

# Se pueden usar números, pero no al principio de las variables
# 1user  # Error
# user1

# No se deben usar como nombres algunas palabras clave especiales:
# print, def, input...
